var TxtType = function (el, toRotate, period) {
    this.toRotate = toRotate;
    this.el = el;
    this.loopNum = 0;
    this.period = parseInt(period, 10) || 2000;
    this.txt = '';
    this.tick();
    this.isDeleting = false;
};

TxtType.prototype.tick = function () {
    var i = this.loopNum % this.toRotate.length;
    var fullTxt = this.toRotate[i];

    if (this.isDeleting) {
        this.txt = fullTxt.substring(0, this.txt.length - 1);
    } else {
        this.txt = fullTxt.substring(0, this.txt.length + 1);
    }

    this.el.innerHTML = this.txt;

    var that = this;
    var delta = 200 - Math.random() * 100;

    if (this.isDeleting) { delta /= 2; }

    if (!this.isDeleting && this.txt === fullTxt) {
        delta = this.period;
        this.isDeleting = true;
    } else if (this.isDeleting && this.txt === '') {
        this.isDeleting = false;
        this.loopNum++;
        delta = 500;
    }

    setTimeout(function () {
        that.tick();
    }, delta);
};

window.onload = function () {
    var elements = document.getElementsByClassName('typewriter');
    for (var i = 0; i < elements.length; i++) {
        var toRotate = elements[i].getAttribute('data-type');
        var period = elements[i].getAttribute('data-period');
        if (toRotate) {
            new TxtType(elements[i], JSON.parse(toRotate), period);
        }
    }
    // INJECT CSS
    var css = document.createElement("style");
    css.type = "text/css";
    css.innerHTML = ".typewriter { border-right: 0.1em solid #fff}";
    document.body.appendChild(css);
};


$(document).ready(() => {
    $('.sidebar a').on('click', (e) => {
        $(e.target).addClass('selected').siblings().removeClass('selected');
        const sectionWrapper = e.target.getAttribute('data-toggle');
        $(sectionWrapper).addClass('visible').siblings().removeClass('visible');
        $(`${sectionWrapper} .header-text`).addClass('slideInLeft').siblings().removeClass('slideInLeft');
        $(`${sectionWrapper} .header-text`).addClass('animated');
    });

    // Carousel
    $('.owl-carousel').owlCarousel({
        items: 2,
        loop: true,
        animateOut: 'slideOutDown',
        animateIn: 'flipInY',
        margin: 30,
        stagePadding: 30,
        smartSpeed: 700,
        autoplay: true,
        autoplayTimeout: 4000,
        autoplayHoverPause: true,
        responsiveClass: true,
        responsive: {
            0: {
                items: 1,
                nav: true
            },
            600: {
                items: 1,
                nav: false
            },
            1000: {
                items: 2,
                nav: false,
                loop: true
            }
        }
    });
});
